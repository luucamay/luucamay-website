import React from "react"

export default ({ children }) => (
  <div style={{ display: `block`}}>
    <small style={{ display: `block`}}>If you read this go for it, your dreams can become true!</small>
    <small style={{ display: `block`}}>If you want to talk to me email me at: <strong>lupemaydana at gmail dot com</strong></small>
    {children}
  </div>
)